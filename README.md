# Siege Lite

Monorepo for a Rainbow Six Siege stats helper.

## Projects

- `overlay/` – Electron overlay app
- `backend/` – Node.js stats backend that talks to R6Data

### Running backend

```bash
cd backend
npm install
npm start
